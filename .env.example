# Copy this file to `.env` and fill in the values. `.env` is gitignored.

# --- Supabase (safe to share) ---
# The publishable (anon) key is protected by Row Level Security and is meant to
# be public, so these two are pre-filled for collaborators.
SUPABASE_URL=https://ukqdjnxdccjauuabpdlq.supabase.co
SUPABASE_ANON_KEY=sb_publishable_zXkWOhK72MIOZd0n3bq2IA_QyEZ4j8Y

# --- eCourts India API (SECRET — do NOT commit a real value) ---
# This is a live, credit-billed key. Get it from the team privately (password
# manager / DM); never paste a real `eci_live_...` value into git.
ECOURTS_API_KEY=eci_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- App ---
APP_URL=http://localhost:8501
